class Solution {
public:
    string defangIPaddr(string add) 
    { string s;
      for(auto c:add)
      {
          if(c=='.')
          {
              s+="[.]";
          }
          else s+=c;
      }
     return s;
    }
};

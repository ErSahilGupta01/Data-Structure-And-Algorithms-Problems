class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        int num=nums.size();
        vector<int> ans(num);
        for(int i=0;i<num;i++){
            ans[i]=nums[nums[i]];
        }
        return ans;
    }
};

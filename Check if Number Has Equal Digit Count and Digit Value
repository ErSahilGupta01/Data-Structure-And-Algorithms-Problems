class Solution:
    def digitCount(self, num: str) -> bool:
        c=0
        for a in range(len(num)):
            if(num.count(str(a))!=int(num[a])):
                c=1
                break
            
                
        if c==1:
            return False
        else:
            return True
        
        
      
